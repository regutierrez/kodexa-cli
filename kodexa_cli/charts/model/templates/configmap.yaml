apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "model.fullname" . }}-configmap
  labels:
    app: kodexa
    resource: model
data:
  model.json: |-
    {{ .Files.Get "resources/model.json" | indent 4}}
  model.zip: |-
    {{ .Files.Get "resources/model.zip" | b64enc }}
