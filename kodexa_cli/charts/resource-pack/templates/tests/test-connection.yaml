apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resource-pack.fullname" . }}-test-connection"
  labels:
    {{- include "resource-pack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resource-pack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
